# 도쿄 여행 일정 웹앱

Henry의 개인 여행용 모바일 웹페이지. 2026년 11월 18일(수)~22일(일), 아카사카 숙소 기준, 문화·예술 컨셉(미술관, 서점, 북카페, LP바). 여행 중 길 위에서 한 손으로 보는 용도가 1순위다. 사용자는 한국어를 쓴다. 모든 UI 문구는 한국어로.

## 현재 상태

- 빌드 과정 없음. 저장소의 이 폴더를 그대로 정적 호스팅하면 된다.
- `index.html` 한 파일에 CSS와 JS가 인라인으로 들어 있다. 일정 데이터는 `<script>` 안 `/* DATA:START */ … /* DATA:END */` 블록의 `HOTEL`, `DAYS`, `CHECKS`, `AIRPORTS`, `LOOP` 다.
- 장소 필드: `t`(시간) `n`(이름) `ja`(일본어) `jaTodo`(일본어 미확인) `kc`(종류 키) `k`(종류 표시) `note` `need`(예약 배지) `ask`(확인 필요 배지) `needId`(예약 체크리스트 연결) `doneLabel` `ok`(현장 발권) `cash` `closed`(휴무) `close`(폐관·마감 시각) `tight`(마감 빠듯) `opt`(선택 일정) `leg`(직전 장소에서 오는 방법) `q`(지도 검색어) `id`(Google place_id) `lat` `lng`. 번호(`num`)와 인덱스(`_idx`)는 실행할 때 자동으로 붙는다.
- `kc` 값: `art` `book` `food` `bar` `market` `view` `hotel` `move`. 색은 `--k-*` 토큰, 아이콘은 `#i-*` 심볼.
- `HOTEL`은 아카사카역 근처 대략값이다. 정확한 숙소 위치는 아직 모름.
- 기능: 하단 고정 날짜 탭, 상단 고정 날짜 바, Leaflet + OpenStreetMap 지도(종류 색 번호 마커, 숙소 마커, 순서대로 잇는 점선, `fitBounds`), 타일을 못 불러오면 SVG 간단 지도로 자동 전환(수동 전환 버튼도 있음), 마커를 누르면 해당 일정으로 스크롤, 장소별 구글 지도·길찾기 버튼, 하루 동선 전체 열기, 일본어 이름 병기와 크게 보기 다이얼로그, 지금·다음 강조(Asia/Tokyo 기준), 선택 일정 접기, 예약 체크리스트(localStorage)와 배지 연동, 일요일 출발 시각 역산, PWA 설치와 오프라인 캐시.
- `sw.js` — 설치할 때 HTML·Leaflet·아이콘·CSV를 캐시(`tokyo-shell-v1`). 지도 타일은 본 것만 `tokyo-tiles-v1`에 최대 900개, 폰트는 `tokyo-fonts-v1`에 담는다. 파일을 고쳐 배포할 땐 `VER`을 올릴 것.
- `vendor/`의 Leaflet 1.9.4는 CDN이 아니라 내려받아 둔 것이다(오프라인 캐시 때문). 업그레이드는 `dist/leaflet.js`, `dist/leaflet.css`, `dist/images/*`를 그대로 교체.
- `data/places.csv`는 Google My Maps 가져오기용. `DAYS`를 바꿨으면 `node tools/make-csv.mjs`로 다시 뽑을 것.

## 할 일

### 남은 것

- **구간 이동 정보(`leg`)** — 검증된 두 구간만 채워져 있다. 토요일 츠키지 → 국립신미술관(히비야선 직통), 국립신미술관 → 네즈(치요다선 노기자카 → 오모테산도). 나머지는 비워 두고 길찾기 버튼만 보여준다. **추측으로 채우지 말 것.** 확인해서 채울 때는 `{line, from, to, note, v:true}` 형식으로, 검증 전이면 `v`를 빼면 "(확인 전)"이 붙는다.
- **기쿠스시 일본어 표기** — `jaTodo:true`로 두었다. Google 지도에서 확인하고 `ja`로 바꿀 것.
- **입장료와 하루 예산** — 공식 사이트에서 확인한 값만, 확인 날짜와 함께 넣을 것. 아직 아무것도 넣지 않았다.
- **숙소 정확한 위치** — 알게 되면 `HOTEL`의 `lat`/`lng`/`n`을 고치고 CSV를 다시 뽑을 것.
- **일요일 비행기 시간** — 페이지의 계산기에 직접 입력하는 방식이라 코드를 고칠 필요는 없다. 다만 긴자 → 공항 이동 시간(하네다 45분, 나리타 80분)은 **추정값**이고 화면에도 그렇게 적어 두었다. 실제 경로로 확인되면 `AIRPORTS`의 `mins`를 고칠 것.
- **스카이트리 구간** — 원래 note에 있던 "두 정거장"은 검증되지 않아 지웠다. 확인되면 `leg`로 넣을 것.

### 끝난 것

1. 실제 지도(Leaflet + OSM, 오프라인이면 SVG 폴백) — 완료
2. 오프라인 PWA(manifest + 서비스 워커, 타일은 본 것만 캐시) — 완료
4. 지금·다음 모드(Asia/Tokyo 고정) — 완료
5. 일본어 이름 병기와 크게 보기 — 기쿠스시 한 곳 빼고 완료
6. 종류별 색·아이콘, 예약 배지 연동, 하단 고정 탭, 44px 버튼, 폐관 시각 표시, 선택 일정 표시·접기, 일요일 역산 칸 — 완료

## 디자인 규칙 (유지)

- 늦가을 도쿄 은행나무 컨셉. 색 토큰은 `:root`에 있는 그대로: 종이 `#ECEEF1`, 표면 `#F7F8FA`, 먹색 `#1E2B4A`, 보조 `#5C6680`, 선 `#C9CED8`, 은행잎 `#E8B620`, 소나무 `#2F5D50`. 다크모드는 `#141A2B` 계열. 종류 색(`--k-*`)은 여기서 파생된 보조색이니 은행잎보다 눈에 띄면 안 된다.
- 글꼴: 제목 Hahmlet(세리프), 본문 IBM Plex Sans KR.
- 하지 말 것: 제목 한 단어만 강조, 대문자 라벨, 모든 섹션에 등장 애니메이션, 똑같은 카드 반복, 링크 끝에 화살표.
- 모바일 390px 기준 먼저, 최대 폭 680px. 다크모드, `prefers-reduced-motion`, 키보드 포커스 유지.
- `color-mix`를 쓸 땐 바로 앞 줄에 단색 폴백을 함께 적을 것(구형 사파리).
- 문구는 짧게, 존댓말, 여행자 입장에서.

## 사실 정보 (2026년 9월 22일 기준 확인, 출발 전 재확인)

- 11월 23일(월)은 일본 공휴일(근로감사의 날). 토·일·월 연휴라 주말이 붐빈다.
- 츠키지 장외시장은 일요일에 대부분 휴무. 그래서 토요일 아침.
- 예약 필수: 아티존(일시 지정제, 웹 매진 시 창구 판매 없음), 네즈(일시 지정, 구매 후 변경·취소 불가, 카드만), 기쿠스시, 이즈모. 스카이트리는 날짜 지정권 권장.
- 현장 발권 가능: 도쿄국립박물관 상설(특별전은 확인), 국립서양미술관, 근대미술관(창구 당일권), 호쿠사이, 와타리움, 국립신미술관(12/6까지 당일권). 시세이도 갤러리 무료.
- 전시: 도쿄국립박물관 대덕사전 10/14–12/6, 히로시게전 9/29–12/20. 근대미술관 다케히사 유메지전 10/23–1/11. 네즈 칠기전 10/24–11/23. 아티존 3개 전시 10/24–1/31.
- 요일 제약: 책거리 수~토만. 난요도 목·일 휴무. 기쿠스시 토·일 휴무. CAVE SHIBUYA 월·일 휴무. 모리오카 서점 13시 개점, 월 휴무. 대부분 미술관 월 휴관, 국립신미술관은 화 휴관.
- 현금: 츠키지, Little Soul Cafe(자리값 500엔).
- 폐관·마감으로 표시한 값은 모두 위 메모에서 온 것이다. 새로 넣을 땐 출처를 확인하고 넣을 것.

## 모르는 것 (Henry에게 물어볼 것)

- 숙소 정확한 이름과 주소
- 공항(나리타/하네다)과 수요일 도착·일요일 출발 시간

## 배포

- GitHub Pages로 정적 배포. 빌드 없이 이 폴더를 그대로 서빙한다.
- 경로는 전부 상대 경로다. `/저장소이름/tokyo-trip/` 같은 하위 경로에서도 서비스 워커 스코프가 맞는다. 절대 경로(`/icons/...`)를 쓰지 말 것.
- OSM 타일 정책상 대량 사전 다운로드는 금지다. "가기 전에" 섹션의 안내(와이파이에서 날짜별로 한 번씩 열어두기)를 유지할 것.

## 완료 기준

- 390px 화면에서 가로 스크롤 없음.
- 오프라인으로 바꾸고 새로고침해도 일정이 열림(지도는 SVG 폴백).
- 날짜 5개 모두 지도, 번호, 일정 순서가 서로 맞음.
- `node tools/make-csv.mjs` 실행 후 `data/places.csv` 갱신 확인.
- 콘솔 에러 없음.
